# 2026-03-12 Minimal Branding

## Scope

- Rename the app to `Texty`.
- Remove the large marketing/branding block from the page.
- Reduce the visible branding to a small `texty` label in the top-left.
- Add a narrow fade at the bottom of the chat history to better indicate overflow.

## Completed

- Updated the document title and description to use `Texty`.
- Removed the hero/marketing block from the main page.
- Simplified the page chrome to a small top-left wordmark.
- Removed the quick-start branding strip from the chat layout.
- Added a pinned bottom fade inside the chat log container to hint at additional scrollable history.

## Result

- The app feels less like a landing page and more like a focused tool.
- Users get a clearer cue that the chat history continues below the fold.
