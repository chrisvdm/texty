# 2026-03-25 Install And Run Doc

- added a dedicated docs page for running the current repo locally as it exists today
- separated the current local RedwoodSDK worker setup from the future hosted CLI direction
- documented the minimal `.dev.vars` setup, `npm run dev`, built-in sandbox routes, and example executor flow
- kept the guide focused on present-day behavior so the docs do not imply the future hosted onboarding already exists
