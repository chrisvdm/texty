# 2026-03-24 Docs Content Rewritten From Build Docs

- rewrote the `/docs` markdown content using the existing build docs and worklogs as source material
- expanded the user docs to cover the real product model instead of placeholder summaries
- documented pinned tools, integration boundaries, executor ownership, and async executor callbacks
- kept the docs aligned to the current shipped MVP language: `account`, `integration`, `executor`, `user_id`, and `channel`
